<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>Doctor's Office</title>
  <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
  <link rel='stylesheet' href='~/css/styles.css' type="text/css">
</head>
<body>
  <div class="container">
    <div class="jumbotron">
    <h1>Doctor's Office</h1>
    </div>
    <h5>Here are the current doctors:</h5><br>
    @if (Model.Count != 0)
    {
      @foreach (var doctor in Model)
      {
        <a href="/doctors/@doctor.GetId()" class="btn btn-dark">@doctor.GetName()</a><br>
      }
    }
  <div id="space">
  </div>
  <hr>
  <a href="/doctors/new" class="btn btn-dark">Add a new doctor</a>
  <a href="/" class="btn btn-dark">Home</a>
  </div>
</body>
</html>
