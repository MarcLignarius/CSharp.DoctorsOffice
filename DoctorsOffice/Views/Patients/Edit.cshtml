<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>Doctor's Office</title>
  <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
  <link rel='stylesheet' href='~/css/styles.css' type="text/css">
</head>
<body>
  <div class="container">
    <div class="jumbotron">
      <h1>Doctor's Office</h1>
    </div>
    <em>You can edit the patient @Model["patient"].GetName() here:</em>
    <form action="/doctors/@Model["doctor"].GetId()/patients/@Model["patient"].GetId()" method="post">
      <label for="newName">Name: </label><br>
      <input type="text" name="newName" value="@Model["patient"].GetName()"><br>
      <label for="newBirthDate">BirthDate: </label><br>
      <input type="date" name="newBirthDate" value="@Model["patient"].GetBirthDate()"><br>
      <button type="submit" class="btn btn-dark">Edit</button>
    </form>
    <div id="space">
    </div>
    <hr>
    <a href="/doctors/@Model["doctor"].GetId()/patients/@Model["patient"].GetId()" class="btn btn-dark">Return to previous page</a>
  </div>
</body>
</html>
